# Frontend (React + Vite)

Simple React UI for the Atom-to-Plant pipeline.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` to your FastAPI server (default `http://localhost:8000`).
